import styled from 'styled-components';

const stc = styled.div`
  display: flex;
  flex:1;
  justify-content: flex-end;
  white-space: pre;
  font-size: 28px;
  opacity: 0.4;
  overflow: hidden;
`;

export default stc;
